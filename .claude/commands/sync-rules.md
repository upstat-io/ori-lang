---
name: sync-rules
description: Sync all .claude/rules/*.md files - DEPRECATED, use /sync-docs instead
allowed-tools: Read
---

# Sync Rules Files — DEPRECATED

**This command has been superseded by `/sync-docs` Phase 2.**

`/sync-docs` performs a comprehensive sync of ALL documentation surfaces including rules files. Phase 2 of `/sync-docs` covers `.claude/rules/*.md` with the same verification discipline — reading source code, verifying against spec clauses, and recording a verification ledger.

Run `/sync-docs` instead.

## User Input

$ARGUMENTS
