# Cargo build artifacts
/target/

# Test-run artifacts emitted by scripts/run-engine-tests.sh,
# scripts/run-smoke-test.sh, scripts/run-coverage-corpus.sh per
# the design-validation gate
# Implementation Items 122 + 126 + 127.
/test-results/

# Lean 4 mirrors emitted by scripts/run-bootstrap-proofs.sh (regenerated
# each run); the hand-authored kernel/ + engines/ proof sources are tracked.
/proofs/01A-bootstrap/lean4-emitted/
