# E5007: Debug Information Creation Failed

The backend could not create debug metadata for the generated module.

## Fix

Retain the detailed backend message and report the source, target, debug-info
settings, and `ori --version` at
`https://github.com/upstat-io/ori-lang/issues`. This is normally a compiler
defect, not an error in the Ori source.
