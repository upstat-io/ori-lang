# E5002: Optimization Pipeline Failed

LLVM could not complete the requested optimization pipeline for the generated
module.

## Fix

Retain the pipeline name and LLVM message shown in the diagnostic. Try the
default optimization level; if the failure remains, report the source, full
diagnostic, target, and `ori --version` at
`https://github.com/upstat-io/ori-lang/issues`.
