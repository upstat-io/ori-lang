# E6080: Feature Not Implemented

The selected execution path does not yet implement the language feature named
by the diagnostic.

## Fix

Use the supported alternative suggested by the diagnostic. If no alternative
exists for valid Ori source, report the source, backend, and `ori --version` at
`https://github.com/upstat-io/ori-lang/issues`.
