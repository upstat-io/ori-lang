# E0009: String Written with Single Quotes

A multi-character value was written between single quotes. Ori uses double
quotes for strings and single quotes only for one character.

## Fix

Write text as `"text"`. Keep single quotes only for character literals such as
`'x'`.

## Spec

- `docs/ori_lang/v2026/spec/07-lexical-elements.md` - sections 7.7.3 and 7.7.5
- `docs/ori_lang/v2026/spec/grammar.ebnf` - `string_lit` and `char_lit`
