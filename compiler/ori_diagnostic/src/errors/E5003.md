# E5003: Output Emission Failed

LLVM could not emit the requested object file, assembly, bitcode, or textual
LLVM IR, or failed while writing that output.

## Fix

Check that the output directory exists and is writable and that the selected
output kind is supported for the target. If those are correct, report the full
backend message with the target and `ori --version`.
