# E5005: Runtime Library Not Found

The compiler could not locate the Ori runtime library needed to link the
program.

## Fix

Install or rebuild the Ori runtime, or select it explicitly with
`ori build --runtime-path=/path/to/lib`. The diagnostic names each path that was
searched; include those paths and `ori --version` in a report if the runtime is
present but was not found.
