# E6006: Size Divided by a Negative Value

A `Size` value was divided by a negative integer. The result cannot be
represented as a size.

## Fix

Require a positive divisor before dividing and handle zero separately.

## Spec

- `docs/ori_lang/v2026/spec/08-types.md` - `Size` arithmetic
