# E6011: Invalid Binary Operator for Type

A binary operator was applied to a type that does not support that operator.

## Fix

Use an operator implemented by the operand type, convert the value explicitly,
or call the appropriate type method.

## Spec

- `docs/ori_lang/v2026/spec/operator-rules.md` - operator typing rules
