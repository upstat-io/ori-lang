# E6026: Key Not Found

E6026 is a dormant compatibility category for a required map key that is
absent. No production caller currently constructs it; built-in map indexing
returns `Option<V>`, where a missing key is `None`.

## Fix

Handle the `None` result from map indexing explicitly instead of requiring a
value for every key.
