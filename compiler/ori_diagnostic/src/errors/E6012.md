# E6012: Binary Operand Type Mismatch

The two operands of a binary expression have incompatible runtime types.

## Fix

Convert one operand explicitly or change the expression so both operands have
the compatible types required by the operator.

## Spec

- `docs/ori_lang/v2026/spec/operator-rules.md` - operator typing rules
