# E6021: Undefined Function at Runtime

Evaluation tried to call a function that is not available in the current
module or imports.

## Fix

Correct the function name, define it, or import it from the module that owns
it. If type checking accepted the call, report the source and `ori --version`.
