# E6099: Runtime Error

Evaluation failed with a runtime condition that has no more specific E6xxx
category. The diagnostic message contains the concrete cause.

## Fix

Follow the action named in the diagnostic message. If the message describes an
internal compiler condition rather than the Ori program, report the source,
full diagnostic, and `ori --version`.
