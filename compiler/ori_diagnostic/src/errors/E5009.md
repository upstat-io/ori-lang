# E5009: Module Target Configuration Failed

The compiler could not apply the selected target triple, data layout, or other
target configuration to the generated module.

## Fix

Select a supported target shown by `ori build --help`. If the target should be
supported, report the requested triple, complete backend message, and
`ori --version` at `https://github.com/upstat-io/ori-lang/issues`.
