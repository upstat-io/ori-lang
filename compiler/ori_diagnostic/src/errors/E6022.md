# E6022: Undefined Constant at Runtime

Evaluation tried to read a constant that is not available in the current scope
or imports.

## Fix

Correct the constant name, declare it with its `$` binding, or import it with
the `$` prefix.

## Spec

- `docs/ori_lang/v2026/spec/12-constants.md` - constants and imports
