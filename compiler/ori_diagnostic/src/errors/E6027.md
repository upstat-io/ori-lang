# E6027: Assignment to Immutable Binding

An assignment targets a binding declared with the immutable `$` prefix.

## Fix

Compute a new value instead of assigning, or remove `$` from a local binding
that is intentionally mutable.

## Spec

- `docs/ori_lang/v2026/spec/12-constants.md` - immutable bindings
