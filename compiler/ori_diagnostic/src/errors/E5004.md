# E5004: Target Not Supported

The requested target cannot be compiled by the active Ori backend, or its
target description could not be created.

## Fix

Run `ori build --help` to review target options, then select a target supported
by the installed compiler. Include the requested target and `ori --version`
when reporting a target that should be supported.
