@count_chars (w: str) -> int = w.length();

@total_items (xs: [str]) -> int = xs.length();

@main () -> int = {
    let a = count_chars(w: "hello");
    let b = total_items(xs: ["a", "b", "c"]);
    if a > b then a else b
}
