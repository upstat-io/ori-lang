@main () -> int = {
    let sum = 0;
    let i = 0;
    loop {
        if i >= 10 then break;
        sum = sum + i;
        i = i + 1;
    };
    // 0+1+2+...+9 = 45
    if sum == 45 then 0 else 1
}
