// Matrix coverage: Result.unwrap with [int] payload
@make () -> Result<[int], str> = Ok([10, 20, 30]);

@main () -> int = {
    let r = make();
    let v = r.unwrap();
    if v == [10, 20, 30] then 0 else 1
}
