// Regression: BUG-04-013 — Option.unwrap must RC-retain heap string payload
@make () -> Option<str> = Some("hello world this is a long heap string");

@main () -> int = {
    let o = make();
    let v = o.unwrap();
    if v == "hello world this is a long heap string" then 0 else 1
}
