#[derive(Eq)]
type Point = { x: int, y: int }

@main () -> int = {
    let a = Point { x: 1, y: 2 };
    let b = Point { x: 1, y: 2 };
    let c = Point { x: 3, y: 4 };
    if a.eq(other: b) && !a.eq(other: c) then 0 else 1
}
