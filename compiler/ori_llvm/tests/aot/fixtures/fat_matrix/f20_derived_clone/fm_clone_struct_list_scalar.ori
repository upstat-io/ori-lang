#derive(Eq, Clone)
type Container = { items: [int] }

@main () -> int = {
    let a = Container { items: [1, 2, 3] };
    let b = a.clone();
    if a == b then {
        if b.items.length() == 3 then 0 else 1
    } else 2
}
