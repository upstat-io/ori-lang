@source () -> [int] = [1, 2, 3];

@main () -> int = {
    // Non-nested flatten: [int] -> [int] unchanged (identity, per the tag-guard).
    let $x = source().flatten();
    if x.length() == 3 then 0 else 1
}
