@main () -> int = {
    let xs = [1, 2, 3];
    let ys = xs.reverse();
    // xs should still be [1,2,3], ys should be [3,2,1]
    if xs.first().unwrap() == 1 && ys.first().unwrap() == 3 then 0 else 1
}
