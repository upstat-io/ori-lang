@main () -> int = {
    let xs = for i in 0..10000 yield i;
    let evens = xs.iter()
        .filter(x -> x % 2 == 0)
        .count();
    // 0,2,4,...,9998 → 5000 items
    if evens == 5000 then 0 else 1
}
