// Cross-type: curried closure capturing str (RC-typed via fat pointer).
// Same root cause as list capture.
@main () -> int = {
    let $fst = a -> b -> a;
    let $s = fst("hello")(0);
    if s == "hello" then 0 else 1
}
