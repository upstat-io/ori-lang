@main () -> int = {
    let outer = catch(
        expr: {
            let opt: Option<int> = None;
            let inner = catch(expr: opt.expect(msg: "inner expect message beyond sso width"));

            match inner {
                Err(_) -> 7,
                Ok(_) -> 3
            }
        },
    );
    match outer {
        Ok(v) -> if v == 7 then 0 else 2,
        Err(_) -> 1
    }
}
