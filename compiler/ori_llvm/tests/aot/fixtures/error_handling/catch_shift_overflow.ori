// Regression: BUG-04-159 — same-frame catch of shift-overflow implicit panic.
// Same emit_panic_block carrier via emit_checked_shift.
@main () -> int = {
    let result = catch(expr: 1 << 64);
    if result.is_err() then 0 else 1
}
