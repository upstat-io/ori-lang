@main () -> int = {
    let xs = [true, false, true];
    let result = catch(expr: xs[7]);
    let still = xs[0];
    match result {
        Err(_) -> if still == true then 0 else 2,
        Ok(_) -> 1
    }
}
