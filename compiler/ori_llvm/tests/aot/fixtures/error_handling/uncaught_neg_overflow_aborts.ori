// Negative pin: negation overflow (-INT_MIN) with NO catch in
// scope MUST still abort. Clamps the emit_unary_op neg dispatch-site boundary.
@main () -> int = {
    let m = -9223372036854775807 - 1;
    let x = -m;
    x
}
