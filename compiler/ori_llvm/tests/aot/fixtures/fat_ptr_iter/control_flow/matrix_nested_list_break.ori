@main () -> int = {
    let nested = [[10, 20, 30], [40, 50], [60, 70, 80, 90]];
    let total = 0;
    for inner in nested do {
        for n in inner do {
            if n > 50 then break;
            total = total + n
        }
    };
    if total == 150 then 0 else 1
}
