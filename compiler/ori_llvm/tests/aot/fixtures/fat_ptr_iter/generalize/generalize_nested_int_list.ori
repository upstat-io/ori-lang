@main () -> int = {
    let nested = [[1, 2, 3], [4, 5], [6, 7, 8, 9]];
    let total = 0;
    for inner in nested do {
        for n in inner do {
            total = total + n
        }
    };
    if total == 45 then 0 else 1
}
