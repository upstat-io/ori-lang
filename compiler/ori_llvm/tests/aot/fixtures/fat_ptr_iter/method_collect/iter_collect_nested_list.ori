@main () -> int = {
    let nested = [[1, 2, 3], [4, 5, 6], [7, 8, 9]];
    let collected: [[int]] = nested.iter().collect();
    // Avoid Option<[int]>.unwrap() — BUG-04-061: missing mono instance for complex generic unwrap
    if collected.len() == 3 then 0 else 1
}
