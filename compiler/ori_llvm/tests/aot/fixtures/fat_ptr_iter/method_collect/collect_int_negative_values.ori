// Negative values test signed narrowing boundary
@main () -> int = {
    let collected = [-1, -128, 127].iter().collect();
    let literal = [-1, -128, 127];
    if collected == literal then 0 else 1
}
