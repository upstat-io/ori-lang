@ack (m: int, n: int) -> int = {
    if m == 0 then n + 1
    else if n == 0 then ack(m: m - 1, n: 1)
    else ack(m: m - 1, n: ack(m: m, n: n - 1))
};

@main () -> int = {
    // ack(3, 3) = 61
    if ack(m: 3, n: 3) == 61 then 0 else 1
}
