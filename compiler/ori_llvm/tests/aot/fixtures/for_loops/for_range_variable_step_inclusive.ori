@make_step () -> int = 2;

@main () -> int = {
    let $step = make_step();
    let sum = 0;
    for i in 0..=10 by step do sum = sum + i;
    // 0 + 2 + 4 + 6 + 8 + 10 = 30
    if sum == 30 then 0 else 1
}
