// 3-hop chain with [T] as the generic type argument.
@id <T> (x: T) -> T = x;
@wrap <T> (x: T) -> T = id(x: x);
@double_wrap <T> (x: T) -> T = wrap(x: x);

@main () -> int = {
    let xs = double_wrap(x: [1, 2, 3]);
    if xs.len() == 3 && xs[0] == 1 && xs[2] == 3 then 0 else 1
}
