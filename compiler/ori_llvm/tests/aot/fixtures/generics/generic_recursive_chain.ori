// Self-recursive generic — each recursive call site is a deferred
// mono resolution on the SAME scheme var.
@countdown <T> (x: T, n: int) -> T =
    if n == 0 then x else countdown(x: x, n: n - 1);

@main () -> int = {
    let n = countdown(x: 42, n: 5);
    if n == 42 then 0 else 1
}
