// 3-hop chain with (T, T) as the generic type argument.
@id <T> (x: T) -> T = x;
@wrap <T> (x: T) -> T = id(x: x);
@double_wrap <T> (x: T) -> T = wrap(x: x);

@main () -> int = {
    let t = double_wrap(x: (3, 4));
    if t.0 == 3 && t.1 == 4 then 0 else 1
}
