@main () -> int = {
    let x = 5;
    let result = if x > 0 then
        match x {
            1 -> 10,
            5 -> 50,
            _ -> 0,
        }
    else 0;
    if result == 50 then 0 else 1
}
