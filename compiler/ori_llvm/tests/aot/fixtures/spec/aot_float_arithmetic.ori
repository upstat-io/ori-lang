@main () -> int = {
    let add = 2.5 + 3.5;
    let sub = 10.0 - 3.75;
    let mul = 3.0 * 4.0;
    let quotient = 15.0 / 2.0;
    if add == 6.0 && sub == 6.25 && mul == 12.0 && quotient == 7.5 then 0 else 1
}
