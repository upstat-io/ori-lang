// Regression: TPR-02-003 — Result<int, str> ?? Err("x") must chain
@main () -> int = {
    let a: Result<int, str> = Err("first");
    let b: Result<int, str> = a ?? Err("fallback");
    match b {
        Ok(v) -> 1,
        Err(e) -> if e == "fallback" then 0 else 2,
    }
}
