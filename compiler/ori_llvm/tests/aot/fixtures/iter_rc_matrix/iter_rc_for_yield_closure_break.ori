@main () -> int = {
    let fns = [x -> x + 1, x -> x * 2, x -> x - 1];
    let idx = 0;
    let result = for f in fns yield {
        if idx == 1 then break;
        let r = f(10);
        idx = idx + 1;
        r
    };
    if result.length() == 1 then 0 else 1
}
