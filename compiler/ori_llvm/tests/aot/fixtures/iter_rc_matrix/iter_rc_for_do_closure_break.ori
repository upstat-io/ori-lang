@main () -> int = {
    let fns = [x -> x + 1, x -> x * 2, x -> x - 1];
    let count = 0;
    for f in fns do {
        if count == 1 then break;
        count = count + 1;
    };
    if count == 1 then 0 else 1
}
