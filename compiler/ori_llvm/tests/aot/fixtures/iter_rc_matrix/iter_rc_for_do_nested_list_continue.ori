@main () -> int = {
    let items = [[1, 2, 3], [4], [7, 8, 9]];
    let count = 0;
    for inner in items do {
        if inner.length() == 1 then continue;
        count = count + 1;
    };
    if count == 2 then 0 else 1
}
