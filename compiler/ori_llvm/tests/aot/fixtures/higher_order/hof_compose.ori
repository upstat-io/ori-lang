@compose (f: (int) -> int, g: (int) -> int, x: int) -> int = f(g(x));

@main () -> int = {
    let dbl = (n: int) -> n * 2;
    let inc = (n: int) -> n + 1;
    // compose(5) = (5+1)*2 = 12
    if compose(f: dbl, g: inc, x: 5) == 12 then 0 else 1
}
