@main () -> int = {
    let a = 1;
    let f = (b: int) -> int = {
        let g = (c: int) -> int = {
            let h = (d: int) -> int = a + b + c + d;
            h(4)
        };
        g(3)
    };
    // 1 + 2 + 3 + 4 = 10
    if f(2) == 10 then 0 else 1
}
