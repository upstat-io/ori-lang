@fold (init: int, f: (int, int) -> int) -> int = {
    let acc = init;
    for i in 1..=5 do {
        acc = f(acc, i);
    };
    acc
}

@main () -> int = {
    let sum = fold(init: 0, f: (a: int, b: int) -> a + b);
    // 0+1+2+3+4+5 = 15
    if sum == 15 then 0 else 1
}
