@sum_list (xs: [int]) -> int = {
    let total = 0;
    for x in xs do {
        total = total + x;
    };
    total
}

@main () -> int = {
    let xs = [10, 20, 30, 40];
    let a = sum_list(xs: xs);
    let b = sum_list(xs: xs);
    if a == 100 && b == 100 then 0 else 1
}
