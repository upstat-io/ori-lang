// Journey 6: Pattern matching + match expression
// Expected: classify(0) + classify(5) + classify(-3) = 0 + 1 + 2 = 3
@classify (n: int) -> int =
  match n {
    0 -> 0,
    x if x > 0 -> 1,
    _ -> 2,
  };

@main () -> int = classify(0) + classify(5) + classify(-3);
