// Journey 5: Structs + field access
// Expected: p.x + p.y = 3 + 4 = 7
type Point = { x: int, y: int };

@sum (p: Point) -> int = p.x + p.y;

@main () -> int = {
  let p = Point { x: 3, y: 4 };
  sum(p)
};
