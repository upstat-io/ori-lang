// Golden test: Generic implementations
// Spec: 08-declarations.md § Implementations
// Design: tooling/formatter/design/02-constructs/declarations.md

type Container<T> = { value: T }

trait Clone {
    @clone (self) -> Self
}

impl<T: Clone> Container<T>: Clone {
    @clone (self) -> Self = Container { value: self.value.clone() }
}
