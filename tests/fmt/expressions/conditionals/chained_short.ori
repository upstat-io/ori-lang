// Chained else-if under 100 chars must STILL break per spec.
// Spec: Annex D §If-Then-Else line 755 - chained `else if` each on own line.
@sign (n: int) -> str =
    if n < 0 then "neg"
    else if n == 0 then "zero"
    else "pos";
