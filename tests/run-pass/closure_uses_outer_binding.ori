// Test that closures can use outer bindings (not self-capture)
// Spec: Clause 21.1.4
//
// A closure using a variable from outer scope is NOT self-capture.
// This should compile and run without error.

@main () -> int = {
    let x = 42;
    let get_x = () -> x;
    get_x()
}
