// Ackermann (call-heavy macro kernel). ackermann(3, n) is exponential in calls.
@ackermann (m: int, n: int) -> int =
    if m == 0 then n + 1
    else if n == 0 then ackermann(m: m - 1, n: 1)
    else ackermann(m: m - 1, n: ackermann(m: m, n: n - 1));

@main () -> int = ackermann(m: 3, n: 6);
