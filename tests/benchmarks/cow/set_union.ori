// Benchmark: Set insert (unique owner)
// Exercises: COW fast path for set — in-place insert when unique
// Expected: O(n) with COW

@main () -> int = {
    let n = 5000;
    let set_a: Set<int> = [0].iter().collect();
    let i = 1;
    loop {
        if i >= n then break;
        set_a = set_a.insert(i);
        i = i + 1;
    };
    if set_a.len() == n then 0 else 1
}
