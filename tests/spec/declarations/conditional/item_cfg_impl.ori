// Spec §25.4: Item-level #cfg on impl blocks
type Logger = { verbose: bool };

#cfg(debug)
impl Logger {
    @dump (self) -> void = ();
}

@main () -> void = print(msg: "item-level cfg on impl accepted");
