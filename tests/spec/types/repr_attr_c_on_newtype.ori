// Invalid: #repr("c") on a newtype — only valid on structs.
#compile_fail(code: "E2041", message: "can only be applied to structs")
@repr_attr_c_on_newtype tests _ () -> void = { () }

#repr("c")
type BadCNewtype = int;
